const search = require('../src/plugins/data-sources/search')
const chai = require('chai')
const expect = chai.expect

describe('search module', () => {
  describe('search', () => {
    it('should fail', () => {
      expect(true).to.equal(false)
    })
  })
})
